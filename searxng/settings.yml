use_default_settings: true

server:
  secret_key: "mobhub-searxng-secret-key-change-me"
  bind_address: "0.0.0.0"
  port: 8080

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "en"
  formats:
    - html
    - json
